<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        h1, h2, h3, h4, h5, h6, label, p, a {
            font-family: 'Montserrat', sans-serif, Arial;
        }

        .logo-container img {
            max-height: 10em;
            max-width: 10em;
        }

        .logo-container p {
            font-size: 1.5rem;
        }

        .title-container h1 {
            font-size: 2.5rem;
        }
    </style>
</head>
<body>
<div id="wrapper" class="d-flex flex-column align-items-center">

    <div class="logo-container text-center py-4">
        <img src="/static/auction_platform_logo.png" alt="Auction platform logo" class="img-fluid">
        <p>Auction platform</p>
    </div>

    <div class="alert-container col-12 col-md-10 col-lg-8">
        {% set error_list = get_flashed_messages() %}
        {% if error_list %}
            {% for error in error_list %}
                <div class="alert alert-danger alert-dismissible fade show" role="alert" aria-label="error:">
                    {{ error }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
        {% endif %}
    </div>

    <div class="title-container col-12 col-md-10 col-lg-8 text-center mb-4">
        <h1>{{ title }}</h1>
    </div>

    <div class="content-container col-12 col-md-10 col-lg-8">
        {% block contents %}{% endblock %}
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
